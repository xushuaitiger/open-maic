# Web framework
fastapi>=0.115.0
# LangGraph multi-agent orchestration
langgraph>=0.2.0
langchain-core>=0.3.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1

# Async HTTP
httpx>=0.27.0

# LLM providers
openai>=1.40.0
anthropic>=0.34.0
google-generativeai>=0.8.0

# Streaming / SSE
sse-starlette>=2.1.0

# ID generation
nanoid>=2.0.0

# PDF parsing (built-in unpdf alternative)
pypdf>=4.3.0

# Web search
# (Tavily via direct HTTP – no extra SDK needed)

# Utilities
python-dateutil>=2.9.0
aiofiles>=24.1.0

# Robust JSON repair / partial JSON parsing for LLM streams
json-repair>=0.30.0

# Settings persistence
aiomysql>=0.2.0          # async MySQL 5.6+ driver (pure-Python, no native libs)
cryptography>=42.0.0     # Fernet AES-128 encryption for stored API keys
